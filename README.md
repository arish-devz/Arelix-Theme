# My Custom Pterodactyl Theme

A fresh start for building a custom Pterodactyl theme.

## 📁 Structure
- `resources/`: Contains your custom views and CSS.
- `install.sh`: Simple script to install your theme.

## 🛠 Development
1. Edit files in `resources/`.
2. Run `./install.sh` to apply changes to your Pterodactyl panel.
